# Output generato dal build (rigenerabile con `python3 build.py`)
dist/

# File dati del singolo cliente (si parte da content.example.json)
content.json

# Esempio della factory: contiene i DATI REALI di Stefano (key-box, Wi-Fi, telefono) —
# NON va in un repo cliente pubblico. Resta nella factory di origine.
content.example.json

# macOS / editor
.DS_Store
*.swp

# Python
__pycache__/
*.pyc

# Tooling locale (anteprima dev / agent) — non fa parte del sito pubblicato
.claude/

# QR dell'URL pubblico (artefatto rigenerabile, da consegnare al cliente)
qr-*.png
